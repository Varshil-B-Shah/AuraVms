# Server Configuration
NODE_ENV=development
PORT=3001

# Gmail SMTP Configuration (for EmailService)
SMTP_HOST=smtp_host
SMTP_PORT=smtp_port
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password

# Email Settings
FROM_EMAIL=your-email@gmail.com
FROM_NAME=Document Approval System
BASE_URL=http://localhost:3001

# Manager Email (receives approval emails)
MANAGER_EMAIL=your-manager-email@gmail.com

# Authentication (Hardcoded Users)
WRITER_EMAIL=writer@example.com
WRITER_PASSWORD=writer_password
MANAGER_EMAIL_AUTH=manager@example.com
MANAGER_PASSWORD=manager_password

# JWT Secret (change in production)
JWT_SECRET=your-secret-key-change-in-production

# Email Security (for secure email links)
EMAIL_SECRET=email-secret-key-for-secure-links-change-in-production
